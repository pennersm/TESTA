# ---------------------------------------------------------------------
# skillet preamble information used by panhandler
# unique snippet name
name: 1UniqueNameofSnippet of Snippet
# label used for menu selection
label: 1LabelIn2ndRow in 2nd row 
description: Description of the deszcription
type: panos
extends:
labels: 1morelabels labels second time
  collection:
    - 1SPcollection the collection
# end of preamble section
# ---------------------------------------------------------------------
# variables section
#
variables:
#
 - name: 11UEIPPool
   description: IP Pool for own subscribers UE
   default: 100.64.0.0/16
   type_hint: text

 - name: 11SFN35ServerIP
   description: IP address of SFN3.5 Server
   default: 10.0.0.1
   type_hint: text

 - name: 11TAPNIC
   description: Interface for TAP zone
   default: ethernet1/1
   type_hint: text
#
# ---------------------------------------------------------------------
# snippets section
# ---------------------------------------------------------------------
# snippets used for api configuration including xpath and element as file name
# files will load in the order listed
snippets:
#
